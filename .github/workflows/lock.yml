name: "Lock Threads"

on:
  schedule:
    - cron: "00 1 * * *"

jobs:
  call-workflow:
    permissions:
      issues: write
      pull-requests: write
    uses: nexus-module/.github/.github/workflows/reusable-lock.yml@main
